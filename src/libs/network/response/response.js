export const normalize = (responseData) => {
  /**
   * TODO : response data 노말라즈 처리
   */
  return responseData;
};

export const error = (responseError, resolve, reject) => {
  /**
   * TODO : response data 에러처리
   */
  return reject(responseError);
};
